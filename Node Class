package project1;

public class Node<T extends Comparable<T>> implements Compartor<T>
    {
   @Override
   public int compare(T a, T b) {
    return a.compareTo(b);
  }
       public T key ;
       public Node<T> next;

        public Node(T key)
        {
            next = null;
            this.key = key;
        }
        
         public Node()
        {
            next = null;
            
        }
         
}
